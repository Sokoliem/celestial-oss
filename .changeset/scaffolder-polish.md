---
'create-celestial': minor
---

Scaffolder polish: non-interactive template selection (`create-celestial my-app tsx-app` or `--template`), `--help`/`--version`, a post-scaffold file summary, and templates that now ship an importable app config, a headless `@celestial/test` smoke suite, and a GitHub Actions CI workflow out of the box.
